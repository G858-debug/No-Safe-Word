[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["rm -rf apps/web/.next/cache && npm run build --workspace=@no-safe-word/web"]

[start]
cmd = "npm run start --workspace=@no-safe-word/web"
